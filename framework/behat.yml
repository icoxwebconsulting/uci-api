default:
    suites:
        local_dev:
            contexts:
                - FeatureContext:
                    baseUrl: http://localhost/app_dev.php/
        local_prod:
            contexts:
                - FeatureContext:
                    baseUrl: http://localhost/
        ci:
            contexts:
                - FeatureContext:
                    baseUrl: http://ci.uci.com/